[project]
name = "token-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "livekit-api",
    "python-dotenv",
]

[tool.uv]
package = false
